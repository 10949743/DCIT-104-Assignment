// Nana Kwame Biney (10949743)
//program for producing the sum and average of prime numbers less than F

#include <iostream>
using namespace std;

bool isPrime(int F)
{
    //condition for F to work
    if(F < 2)
        return false;

    //checking for prime number
    for(int a = 2; a < F; a++)
        if(F % == 0)
            return false;

    return true;
}

//printing the prime numbers less than F
void printprime(int F)
{
    cout <<"The prime numbers are:";
    for(int a = 2; a  < F; a++)
        if(isPrime(j))
            cout << a <<"";

}

//sum of prime numbers less than F
int main()
{
    int (sum = 0)
    sum += a
    a++
}

//average of prime numbers
float average = sum/a
